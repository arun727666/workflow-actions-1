name: Send Low Stock Report

on:
  schedule:
    - cron: '30 12 * * *'  # Runs every day at 6:00 PM IST (12:30 PM UTC)

jobs:
  send-report:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Run Flask low stock report
        run: |
          # Install Python and dependencies
          python3 -m pip install --upgrade pip
          pip install requests boto3 python-telegram-bot

          # Run your script or curl your Flask endpoint
          curl http://your-flask-app-url/send-low-stock
